# user-service

Multi-tenant user management service (SQLite-per-tenant, FastAPI, FastMCP).

This README provides a minimal placeholder so the packaging build backend (hatchling) can locate the README file during `uv sync`/editable build.

Usage (development):

uv run uvicorn src.main:app --reload --port 8000
